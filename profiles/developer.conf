# profiles/developer.conf — the default workstation overlay.
AI_HARNESSES="claude-code,codex,opencode"
AI_OPT_OUT_TELEMETRY=1
# fnm, rustup and uv publish no packaged alternative, so the script backend is
# needed for a full developer install. Review lib/backends/script.sh first.
AI_ALLOW_REMOTE_SCRIPTS=1
