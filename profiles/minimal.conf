# profiles/minimal.conf — environment overlay for the `minimal` profile.
# Component membership lives in manifest/components.yaml.
AI_HARNESSES="claude-code"
AI_OPT_OUT_TELEMETRY=1
AI_ALLOW_REMOTE_SCRIPTS=0
